<!-- Панель навигации с использованием Bootstrap -->
<nav class="navbar navbar-expand-lg navbar-light bg-menu">
    <div class="container">
        <!-- Бренд и кнопка переключения на мобильных устройствах -->
        <a class="navbar-brand" href="{{ path('cronv-tm-bundle') }}">Главная</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-start" id="navbarNav">
            <ul class="navbar-nav">
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" id="navbarDropdown" data-bs-toggle="dropdown" aria-expanded="false">
                            Анкетирование
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown" data-bs-auto-close="false">
                            <li><a class="dropdown-item" href="{{ path('ctb-survey-admin') }}">Анкеты</a></li>
                            <li><a class="dropdown-item" href="{{ path('ctb-survey') }}">Опрос</a></li>
                            <li><a class="dropdown-item" href="{{ path('ctb-survey-admin-am') }}">Назначение</a></li>
                            <li><a class="dropdown-item" href="{{ path('ctb-survey-statistics') }}">Статистика</a></li>
                            <!-- Добавьте другие пункты меню здесь -->
                        </ul>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('ctb-survey-admin') }}">Опрос</a>
                    </li>
                {% endif %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" id="navbarDropdown" data-bs-toggle="dropdown" aria-expanded="false">
                        Расходы/Доходы
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown" data-bs-auto-close="false">
                        <li><a class="dropdown-item" href="{{ path('ctb-transaction-index') }}">Информация</a></li>
                        <li><a class="dropdown-item" href="{{ path('ctb-transaction') }}">Транзакции</a></li>
                        {% if is_granted('ROLE_ADMIN') %}
                            <li><a class="dropdown-item" href="{{ path('ctb-transaction-type') }}">Типы транзакций</a></li>
                        {% endif %}
                    </ul>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('cronv-tm-bundle') }}">Инструкция</a>
                </li>
            </ul>
        </div>
        <!-- Ссылки навигации -->
        <div class="collapse navbar-collapse justify-content-end" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <i class="fa-solid fa-user"></i>
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdownMenuLink">
                        <li><a class="dropdown-item" href="{{ path('app_logout') }}">Мой профиль</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_logout') }}">Настройки</a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item" href="{{ path('app_logout') }}">Выйти</a></li>
                    </ul>
                </li>
            </div>
        </div>
    </div>
</nav>
